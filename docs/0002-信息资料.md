# 信息资料

## 一、SSH 访问

1. 家里win11访问 hp-linux
```
ssh hp-linux-ts
110119test
```

## 二、hp-linux 现状记录(2026-06-27 摸底)

### 1. 用户与目录
- 登录用户:`vichel`
- `$HOME` = `/home/vichel`(项目根目录候选)
- 项目实际位置:`/home/vichel/code/person/deploy-lab/`
  - 当前结构:`server/db/init.sql`
- 注意:`~` 下同时存在 `code/` 和 `deploy-lab/` 两个目录,部署时确认用哪个(目前代码在 `code/person/deploy-lab/` 下)

### 2. 磁盘空间
- 根分区 `/dev/nvme0n1p2`:总 915G,已用 22G,**可用 847G**(3%)
- 空间充足,项目代码与数据放哪都不受限

### 3. Docker 环境
- Docker Server Version:**29.1.3**
- Docker Compose:**2.40.3+ds1-0ubuntu1~24.04.1**
- 版本都够新,compose 文件可用最新语法

### 4. Docker 网络
| NAME | DRIVER | 备注 |
|------|--------|------|
| bridge | bridge | 默认 |
| **docker_app-network** | bridge | **已存在的网络,后面 compose 可考虑复用** |
| host | host | |
| none | null | |

### 5. 现有数据库结构(init.sql)
- 数据库:`deploy_lab`(utf8mb4 / utf8mb4_unicode_ci)
- 表:`tasks`
  ```sql
  id         INT PRIMARY KEY AUTO_INCREMENT
  title      VARCHAR(255) NOT NULL
  status     VARCHAR(50)  NOT NULL DEFAULT 'todo'
  created_at TIMESTAMP    DEFAULT CURRENT_TIMESTAMP
  ```
